# MindSync AI — מצב הפרויקט והחלטות מפתח

מסמך העברה להמשך עבודה בשיחה חדשה. **מעודכן אחרי סבב עבודה שהוסיף הפרדת משתמשים מלאה, ~25 באגים אמיתיים, ואבטחה.**

---

## מה זה

אפליקציית עוזר למידה לסטודנטים. Electron בצד לקוח, Express + MongoDB בצד שרת, AI דרך Gemini (ענן) עם Ollama כגיבוי מקומי. **מרובת משתמשים** — כל משתמש נרשם עם email/סיסמה, הדאטה שלו מופרד לגמרי.

**התזה המרכזית של המוצר:**
> MindSync הוא הדבר שאומר לך את האמת על מה שאתה באמת יודע.

זו לא סיסמה — היא מסננת החלטות. השאלה שצריך לשאול על כל פיצ'ר: *האם זה עוזר למישהו לדעת את האמת על הידע שלו?*

**הבעיה שהיא פותרת:** קריאת סיכום יוצרת תחושת הבנה שלא נבדקה. צ'אט בוט **מחמיר** את זה — ככל שההסבר שוטף יותר, האשליה חזקה יותר. המוצר מודד את הפער בין ביטחון סובייקטיבי לדיוק בפועל.

**מיצוב תחרותי:** הכל במקום אחד. לכל מתחרה יש רכיב אחד; אין אחד שמאגד הכל.

---

## ארכיטקטורה

```
client/    Electron — UI, AI, Google Calendar, חוסם אפליקציות
server/    Express + Mongoose — "קופסה שחורה", בעלת כל הנתונים, מרובת משתמשים
```

**קבצי לקוח מרכזיים:**
| קובץ | תפקיד |
|---|---|
| `main.js` | תהליך ראשי, כל ה-IPC, פרומפטים, פילטרים |
| `renderer.js` | כל ה-UI (~3,400 שורות) |
| `apiClient.js` | עטיפת REST לשרת. **ברירת המחדל היא ה-URL של Render בפרודקשן**, לא localhost — `.env` הוא override לפיתוח בלבד |
| `authClient.js` | **חדש.** שומר session/token מקומית (`auth-session.json` ב-userData), אותו pattern כמו `aiProvider.js` |
| `aiProvider.js` | הפשטה מעל Gemini / Ollama, כולל fallback דו-כיווני עם timeout על שניהם |
| `logger.js` | **חדש.** מראה `console.error`/`warn` גם לקובץ (`mindsync.log` ב-userData) — כי לאפליקציה ארוזה (exe) אין טרמינל, וזה מה ש-Settings' "Copy diagnostic log" קורא |
| `pdfExtract.js` | חילוץ טקסט מודע-RTL (גיבוי בלבד) |
| `pdfRender.js` | רינדור עמודים לתמונות (גיבוי) |
| `tokens.css` / `themes.css` / `ui-kit.css` / `styles.css` | מערכת עיצוב |
| `icons.js` / `ui.js` | אייקוני SVG, toasts, מודאלים |

**מודלים בשרת:** `User` (חדש, מחליף את `Profile`), `Task`, `Event`, `Folder`, `FileItem`, `Settings` (עכשיו per-user, לא singleton), `StudyItem`
**Utils:** `singleton.js` (עדיין רלוונטי רק אם עוד יהיה משהו גלובלי אמיתי), `scheduler.js`
**Middleware:** `asyncHandler.js`, `ApiError.js`, `errorHandler.js`, `auth.js` (**חדש** — `requireAuth`, על כל route)

**GitHub:** קוד השרת ב-`dorharel43/MindSync-Server` (מחובר ל-Render, Manual Deploy — לא auto-deploy). קוד ה-client גם ב-git, ב-repo נפרד.

---

## החלטות מפתח — אל תבטלו אותן בלי סיבה

### 1. AI לא כותב תשובות. הוא מאתר אותן.

התשובה לשאלת `recall` היא **ציטוט מדויק מחומר הקורס**, לא ניסוח של המודל.

**למה:** כשהמודל כתב תשובות הוא ייצר "two-pointer technique" למציאת מקסימום, ו"גרעין לוגי" במקום "גרירה לוגית". במוצר שכל התזה שלו היא אמת, **תשובה שגויה בביטחון גרועה מאין תשובה** — היא מלמדת שטויות ואז בודקת אותך עליהן.

תפקיד המודל צומצם מ"לדעת את החומר" ל"למצוא את הקטע הרלוונטי".

### 2. קריאת PDF ישירות, לא חילוץ טקסט

`generateFromPdf()` שולח את הקובץ עצמו ל-Gemini.

**למה:** כל בעיית איכות שנלחמנו בה — נוסחאות מרוסקות, `חרילן פסכו`, פסוקיות הפוכות, מספור שנדד לאמצע משפט — נבעה מחילוץ טקסט. חילוץ משטח את העמוד ומאבד את מה שנושא משמעות: מבנה נוסחה, אינדקסים, הזחה בקוד, סדר RTL. **אין מודל שישחזר מידע שלא הגיע אליו.**

`pdfExtract.js` ו-`pdfRender.js` נשארים לגיבוי במצב Ollama בלבד.

### 3. שלושה מקורות תשובה, מסומנים בממשק

| `solutionSource` | מתי | תווית |
|---|---|---|
| `document` | ציטוט / פתרון מהמסמך | "From your material" |
| `ai` | Gemini פתר תרגיל בלי פתרון | ⚡ אמבר: "AI-generated — worth checking" |
| `user` | הסטודנט שמר פתרון | "Your solution" |

**הסימון הוויזואלי אינו קישוט.** הסתרת ההבדל היא בדיוק מה שהמוצר נלחם בו.

### 4. כיול ביטחון — הפיצ'ר המבדל

הסדר קריטי: **שאלה → הצהרת ביטחון → חשיפת תשובה → דירוג עצמי.**

ביטחון שנשאל *אחרי* התשובה חסר ערך — כולם מדווחים שידעו.

התזמון (`scheduler.js`) גוזר ציון מ**שילוב** תוצאה+ביטחון:
- צדק + בטוח = 5
- צדק + ניחש = 3 (מזל, חוזר מהר)
- **טעה + בטוח = 0** — חוזר תוך 10 דקות. תפיסה מוטעית מוחזקת בביטחון היא המקרה המסוכן.

### 5. מסך אישור לפני כניסה לדק

שאלות שנוצרו הן **טיוטה**. המשתמש מסנן, עורך, ומאשר.

**למה:** אחרי 8 סבבי פילטרים — כל אחד פתר מקרה וחשף שניים. אין סט כללים שיהיה שלם. עין אנושית תופסת הכל בשניות. זה הופך כשל מקטלני לזניח.

תשובות מקופלות כברירת מחדל (לא לשרוף את ניסיון השליפה הראשון); פתרונות AI נפתחים אוטומטית.

### 6. עברית מטופלת ב-keywords לפני AI

ימים (`hebrewDayMap`) וסוגי אירועים (`classifyEventByKeywords`) — זיהוי דטרמיניסטי, AI רק כגיבוי.

**למה:** מודל קטן שמתבקש לתרגם+להסיק בו-זמנית נכשל. "מבחן" סווג כ-personal, "בדיקה" כ-exam. זיהוי מילות מפתח משעמם אבל מדויק.

**מלכודת (חזרה על עצמה כמה פעמים בפועל):** `\b` ב-JavaScript מוגדר על `[A-Za-z0-9_]` ו**לא מכיר עברית**. כל regex עם `\b` סביב מילה עברית נכשל בשקט. להשתמש ב-lookarounds מפורשים (`(?<![א-ת])מילה(?![א-ת])`). זו הייתה הסיבה לכמה באגי "הטקסט לא מתנקה" נפרדים — כל פעם שמישהו כתב `\bמחר\b` זה נכשל בשקט.

**מלכודת נוספת שנתפסה:** שעה "יבשה" (`ב9`, `ב 9`, `ב-9`) לא הייתה מזוהה בכלל — רק `HH:MM` עם נקודתיים. וגם `וחצי`/`ורבע` (חצי/רבע שעה) לא היו מטופלים כלל. שניהם תוקנו.

### 7. משימות ≠ שאלות מבחן

משימה = משהו עם **דדליין או הגשה**. "פתור שאלה 3" הוא תרגול, לא פריט ברשימת מטלות — הוא קובר דדליינים אמיתיים מתחת לעשרות תרגילים.

### 8. מבחן קניגסברג

**"האם מרצה היה שם את זה במבחן?"** אם לא — לא שואלים. סיפורים היסטוריים מניעים את המושג; המושג נבחן.

### 9. הפרדת משתמשים — `userId` בכל מקום, לא singleton

**חדש הסבב הזה.** כל מודל (`Task`, `Event`, `Folder`, `FileItem`, `Settings`, `StudyItem`) מחזיק `userId`, וכל route (`requireAuth` middleware) מסנן/מצמיד אותו. `Profile` הישן (singleton) הוחלף ב-`User` (name/degree חיים שם עכשיו, לא במסמך נפרד).

**למה זה קריטי:** בלי זה, `Task.find()` (וכו') מחזיר **את כל המשתמשים ביחד**. זה היה חוסם ההפצה המרכזי מהגרסה הקודמת של המסמך הזה.

**שני באגי אבטחה אמיתיים שנתפסו תוך כדי, לא רק "תוספת userId":**
- `Folder.name` היה `unique: true` **גלובלית** — משתמש שני שיוצר תיקייה בשם קיים אצל מישהו אחר היה מקבל 500. תוקן לאינדקס ייחודי מורכב `{userId, name}`.
- `bulk-delete`/`hard-reset` ב-study.js ו-admin.js לא היו מסוננים לפי משתמש בכלל — "Reset all data" של אחד היה מוחק את כולם.

**באג ניתוב קריטי שנתפס:** `/api/auth` היה רשום **אחרי** ה-`notFound`/`errorHandler` middleware ב-`server.js`. ב-Express הסדר קובע — כל בקשה ל-`/api/auth/*` הייתה נתקלת ב-404 לפני שהגיעה בכלל ל-router. register/login היו שבורים לגמרי עד שהסדר תוקן.

### 10. Timeout על כל קריאת רשת — לא אופציונלי

**חדש הסבב הזה, ותבנית שחזרה על עצמה שוב ושוב.** `main.js` הוא **single-threaded** — קריאת רשת תקועה לא רק נכשלת, היא **קופאת את כל האפליקציה**. נמצאו ותוקנו 4 מקומות נפרדים בלי timeout: קריאת Gemini, `apiClient.js`'s `request()` (השרת), ושתי קריאות Google Calendar (`insert`/`delete`). כל אחת הייתה תקיעה שקטה ואינסופית עד שהתגלתה בנפרד.

**כלל אצבע להמשך:** כל `fetch`/קריאת HTTP חדשה שנוספת ל-`main.js` — לשאול "מה קורה אם זה פשוט לא עונה?" ולהוסיף `AbortController`/`timeout` מההתחלה, לא אחרי שמישהו נתקע.

### 11. AI provider fallback — Gemini קודם, Ollama תמיד כגיבוי אמיתי

`generateText()` ב-`aiProvider.js` מנסה Gemini, ובנפילה — Ollama. **חייב** לעבור דרך הפונקציה הזו; `main.js` בעבר דיבר עם Ollama ישירות (`callAIWithFallback` הישנה), מה שהפך את השם שלה לשקר — "Add Task עובד אצלי, לא אצל החבר" היה בדיוק זה: החבר לא היה עם Ollama, וה"fallback" בכלל לא ניסה Gemini.

**עוד תיקון קשור:** כשגם Gemini וגם ה-fallback נכשלים, ההודעה המקורית של Gemini הייתה נזרקת — המשתמש ראה רק "Ollama is not reachable", מבלבל למי שאף פעם לא הגדיר Ollama. עכשיו שתי הסיבות מגיעות יחד.

**מגבלה חשובה לזכור:** Gemini free tier = **20 בקשות ביום** למפתח. כל משתמש עם המפתח שלו (BYOK), אז זה quota אישי — אבל נגמר מהר בבדיקה פעילה (extract-tasks על מסמך אחד = כמה קריאות בגלל chunking).

---

## פילטרים ב-`main.js` (כולם על תוכן שנוצר)

| פונקציה | מה נחסם |
|---|---|
| `isSelfContained()` | "הפונקציה שתוכתב", "לפי הטקסט" — תלוי-הקשר |
| `isHistoricalTrivia()` | "באיזו עיר", "מי הוכיח", "מתי להגיש" |
| `needsReasoningNotQuote()` | "הסבר את תשובתך" — ציטוט לא יכול לנמק |
| `isExerciseTrivia()` | "מהו סוג הנתונים", "האם מותר לשנות" |
| `looksLikeOutline()` | שקופיות אג'נדה |
| `looksLikeHeading()` | כותרות שקופית כתשובה |
| `analyseAnswerText()` | נזק גופן, נוסחה בלבד, נוסח תרגיל |
| `quoteAnswersQuestion()` | סמל בשאלה חסר בציטוט |
| `cleanMathNotation()` | LaTeX → סמלים אמיתיים |
| `parseDurationMinutes()` | **חדש.** "3 שעות"/"שעתיים"/"חצי שעה" → דקות, למתכנן השבועי |

**אזהרה:** ב-`cleanMathNotation` המפה **לא** מכילה בקסלאשים — הם מתווספים בבניית ה-regex במקום אחד. `'\\in'` במחרוזת JS הופך ל-`\i` שהוא `i`, וזה החליף את המילה `Main` ב-`Ma∈` בקוד Java.

---

## מצב איכות לפי סוג חומר

| חומר | תוצאה |
|---|---|
| סטטיסטיקה, ארכיטקטורות, תורת גרפים | ✅ מצוין |
| אלגברה לינארית | ✅ עובד (נכשל ב-100% לפני קריאת PDF ישירה) |
| תכנות מונחה עצמים | ✅ עובד |
| לוגיקה עתירת נוסחאות | ⚠️ חלקי |

---

## מה נשאר

**חוסם הפצה שנותר:**
1. `nodeIntegration: true` + `contextIsolation: false` ב-`main.js` — עדיין קיים ארכיטקטונית. **חמישה חורי XSS קונקרטיים שנוצלו בפועל** (event/folder/file names) תוקנו עם `escapeHtml` הסבב הזה, ונסרקו כל 59 מופעי `innerHTML` ב-`renderer.js` — אבל ה-flag עצמו לא שונה. נדרש `preload.js` + `contextBridge` לתיקון אמיתי של השורש.

**שיפורים שנרשמו אבל לא בוצעו:**
2. RTL בממשק
3. נגישות (אפס `aria-label`)
4. פיצול `renderer.js`
5. ייצוא/גיבוי
6. מיון משימות
7. תיעוד במוצר: עובד הכי טוב עם סיכומי הרצאות
8. Rate limiting על `/api/auth/login` ו-`/register` — אין כרגע, אפשר לנסות אלפי סיסמאות בלי חסימה
9. MongoDB Atlas: לוודא Network Access לא פתוח ל-0.0.0.0/0 בלי צורך, ושמשתמש ה-DB לא Atlas Admin
10. בדיקה ש-`.env` אף פעם לא נכנס להיסטוריית git (`git log --all --full-history -- .env`)
11. Google Calendar sync — אושר ע"י המשתמש שעובד, **לא נבדק שיטתית** אחרי כל שינויי ה-auth/timeout
12. "Select all" על checklist steps בתוך משימה בודדת — דווח כבאג, לא אותר בקוד, ממתין לצילום מסך
13. הפצת Mac אמיתית (`.dmg` חתום) — כרגע רק "הרצה מהמקור" עובד; חתימה/notarization של אפל נדרשים להפצה רחבה

**הוסר בכוונה (לא "נשכח"):**
- XP/Levels/Streak — `/api/stats` הוסר מהשרת, ה-UI המקביל הוסר מה-client (Progress page, sidebar chip)
- בחירת AI provider ידנית, קיצור מקלדת `/`, Notifications toggle — כל השלושה הוסרו לפי בקשה

---

## הפעלה

```bash
cd server && npm install && npm run dev   # חייב לרוץ ראשון (מקומי) — או שה-client מדבר עם Render ישירות
cd client && npm install && npm start
```

**דרוש בשרת (לא בגיט):** `.env` עם `MONGO_URI` **ו-`JWT_SECRET`** (חדש — השרת מסרב לעלות בלי זה). חבילות: `jsonwebtoken`, `bcrypt` (חייבות להיות ב-`package.json`, לא רק מותקנות מקומית, אחרת ה-build ב-Render נכשל).

**דרוש בלקוח (לא בגיט):** `credentials.json`, `token.json` (Google Calendar). `.env` עם `MINDSYNC_SERVER_URL` הוא **אופציונלי עכשיו** — ברירת המחדל היא Render בפרודקשן; להגדיר רק אם רוצים לדבר עם שרת מקומי לפיתוח. **חובה לכלול את ה-`/api`** בסוף ה-URL (`.../api`, לא רק השורש) — שכחה גרמה ל-"Route not found" מספר פעמים.

**Gemini:** מפתח מ-aistudio.google.com/apikey → Settings → AI engine → Test → Save.
פורמט `AQ.` הוא הנוכחי; `AIza` הישן נדחה מספטמבר 2026. **אין לאמת לפי קידומת.**

**Gemini 3.x:** מתעלם מ-`temperature`, זורק שגיאה על penalty params, והחשיבה נגבית מתקציב הפלט (`maxOutputTokens` נדיב + `thinkingLevel`). `mediaResolution: HIGH` חיוני לקריאת אינדקסים.

**דיבאג בפרודקשן:** אין טרמינל באפליקציה ארוזה. Settings → "Copy diagnostic log" מעתיק ללוח את tail של `mindsync.log` (ב-userData, נכתב ע"י `logger.js`).

---

## מלכודת חוזרת: models/ מול routes/

**קרתה שלוש פעמים בפועל הסבב הזה** (`auth.js`, `Event.js`/`events.js`, `Task.js`/`tasks.js`) — קבצים עם שם דומה הוחלפו בין `models/` ל-`routes/`, מה שגרם לתקיעות שקטות (`Event.find is not a function` וכו', כי router הוחלף ב-`require`).

**כלל:** מתחיל ב-`const mongoose = require('mongoose')`, מסתיים ב-`module.exports = mongoose.model(...)` → `models/`. מתחיל ב-`const express = require('express')`, מסתיים ב-`module.exports = router` → `routes/`. יחיד (`Task`, `Event`, `User`) = models; רבים (`tasks`, `events`) = routes.

---

## איך לעבוד

באגים נתפסו **כמעט תמיד** כשהמשתמש הריץ בפועל ודיווח מדויק — כולל לוגים מהטרמינל, לא רק תיאור. כשתיאור לא מסתדר עם מה שהקוד אמור לעשות, לבקש את הלוג/השגיאה המדויקים לפני שמנחשים שוב — ניחושים חוזרים בלי מידע חדש בזבזו סבבים.

לבנות דבר אחד, לבדוק, ואז להמשיך — לא לערום כמה יחד. גם כשמתבקש "לעבור על הכל ולמצוא עוד דברים" — לתעדף ממצאים קונקרטיים על פני רשימה ארוכה של ניחושים.

המשתמש סטודנט למערכות מידע, מתכנת מתחיל. לענות בעברית, קוד באנגלית. הסברים ספציפיים, בלי להניח ידע מובן מאליו.