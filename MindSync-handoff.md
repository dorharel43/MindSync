# MindSync AI — מצב הפרויקט והחלטות מפתח

מסמך העברה להמשך עבודה בשיחה חדשה. עודכן לאחר סבב עבודה על מסך ה-Study, יציבות מול Gemini, וביצועים.

---

## מה זה

אפליקציית עוזר למידה לסטודנטים. Electron בצד לקוח, Express + MongoDB בצד שרת, AI דרך Gemini (ענן) עם Ollama כגיבוי מקומי.

**התזה המרכזית של המוצר:**
> MindSync הוא הדבר שאומר לך את האמת על מה שאתה באמת יודע.

זו לא סיסמה — היא מסננת החלטות. השאלה שצריך לשאול על כל פיצ'ר: *האם זה עוזר למישהו לדעת את האמת על הידע שלו?*

**הבעיה שהיא פותרת:** קריאת סיכום יוצרת תחושת הבנה שלא נבדקה. צ'אט בוט **מחמיר** את זה — ככל שההסבר שוטף יותר, האשליה חזקה יותר. המוצר מודד את הפער בין ביטחון סובייקטיבי לדיוק בפועל.

**מה צ'אטבוט לא יכול לעשות** (שלוש הנקודות שכל פיצ'ר חדש צריך לשבת על אחת מהן):
1. לצבור תיעוד של הטעויות שלך לאורך זמן — כל `review` שנשמר מגדיל את הערך של המוצר ולא של הצ'אטבוט
2. **לסרב לענות** — מסך הביטחון הוא התחייבות כפויה לפני החשיפה; מול צ'אטבוט התשובה תמיד במרחק הודעה
3. לדעת מה **המרצה שלך** אמר — `solutionSource: 'document'`

---

## ארכיטקטורה

```
client/    Electron — UI, AI, Google Calendar, חוסם אפליקציות
server/    Express + Mongoose — "קופסה שחורה", בעלת כל הנתונים
```

**קבצי לקוח מרכזיים:**
| קובץ | תפקיד |
|---|---|
| `main.js` | תהליך ראשי, כל ה-IPC, פרומפטים, פילטרים, תיקון JSON |
| `renderer.js` | כל ה-UI (~3,900 שורות) |
| `apiClient.js` | עטיפת REST לשרת |
| `aiProvider.js` | הפשטה מעל Gemini / Ollama, retry ושרשרת מודלים |
| `pdfExtract.js` / `pdfRender.js` | גיבוי בלבד (Ollama) |
| `tokens.css` / `themes.css` / `ui-kit.css` / `styles.css` | מערכת עיצוב |
| `icons.js` / `ui.js` | אייקוני SVG, toasts, מודאלים |

**מודלים בשרת:** `Task`, `Event`, `Folder`, `FileItem`, `Profile`, `Stats`, `Settings`, `StudyItem`
**Utils:** `singleton.js`, `scheduler.js`

---

## החלטות מפתח — אל תבטלו אותן בלי סיבה

### 1. AI לא כותב תשובות. הוא מאתר אותן.

התשובה לשאלת `recall` היא **ציטוט מדויק מחומר הקורס**, לא ניסוח של המודל.

**למה:** כשהמודל כתב תשובות הוא ייצר "two-pointer technique" למציאת מקסימום, ו"גרעין לוגי" במקום "גרירה לוגית". במוצר שכל התזה שלו היא אמת, **תשובה שגויה בביטחון גרועה מאין תשובה**.

### 2. קריאת PDF ישירות, לא חילוץ טקסט

`generateFromPdf()` שולח את הקובץ עצמו ל-Gemini. כל בעיית איכות שנלחמנו בה נבעה מחילוץ טקסט. **אין מודל שישחזר מידע שלא הגיע אליו.**

### 3. שלושה מקורות תשובה, מסומנים בממשק

| `solutionSource` | מתי | תווית |
|---|---|---|
| `document` | ציטוט / פתרון מהמסמך | "From your material" |
| `ai` | Gemini פתר תרגיל בלי פתרון | ⚡ אמבר: "AI-generated — worth checking" |
| `user` | הסטודנט שמר פתרון | "Your solution" |

### 4. כיול ביטחון — הפיצ'ר המבדל

הסדר קריטי: **שאלה → הצהרת ביטחון → חשיפת תשובה → דירוג עצמי.**
ביטחון שנשאל *אחרי* התשובה חסר ערך.

התזמון (`scheduler.js`) גוזר ציון מ**שילוב** תוצאה+ביטחון:
צדק+בטוח = 5 · צדק+ניחש = 3 · **טעה+בטוח = 0** (חוזר תוך 10 דקות).

### 5. מסך אישור לפני כניסה לדק

שאלות שנוצרו הן **טיוטה**. אין סט כללים שיהיה שלם; עין אנושית תופסת הכל בשניות.

### 6. עברית מטופלת ב-keywords לפני AI

**מלכודת:** `\b` ב-JavaScript מוגדר על `[A-Za-z0-9_]` ו**לא מכיר עברית**. כל regex עם `\b` סביב מילה עברית נכשל בשקט.

### 7. משימות ≠ שאלות מבחן

משימה = משהו עם **דדליין או הגשה**.

### 8. מבחן קניגסברג

**"האם מרצה היה שם את זה במבחן?"** אם לא — לא שואלים.

### 9. נושא אחד בכל פעם — לא ערבוב  *(חדש)*

ברירת המחדל היא הנושא עם הכי הרבה שאלות ממתינות, לא "All subjects".

**למה:** שקלנו interleaving (ערבוב נושאים מחזק שליפה) ופסלנו. מבחן הוא בנושא אחד ספציפי; ערבוב רק מבלבל כשלומדים למבחן בעוד שלושה ימים. **החלטה של המשתמש, לא שלי.**

### 10. מספר שמבוסס על מדגם קטן מדי לא מוצג  *(חדש)*

כל פאנל סטטיסטי מחזיק סף מינימלי, ומתחתיו **שותק או מסביר למה הוא שותק**:

| פאנל | סף |
|---|---|
| כיול (`renderCalibration`) | `CALIBRATION_MIN_REVIEWS = 5` ביקורות |
| כיול לפי נושא (שרת) | `SUBJECT_MIN_REVIEWS = 5`, `SUBJECT_MIN_SURE = 4` |
| מגמה (`sureTrend`) | 5 תשובות "בטוח" בכל חצי |
| מהירות (`pacePattern`) | 12 ביקורות, שליש איטי ≥15ש', פער ≥10ש' |

הלקוח והשרת **חייבים להישאר מיושרים**. כשהלקוח היה על 5 והשרת על 6, נושא עם 5 ביקורות נפל ל-fallback והציג את כיול **כל הדק** בזמן שנושא בודד מסומן.

`pacePattern` מחזיר `{ tooUniform: true }` ולא `null` כשיש מספיק נתונים אבל אין פיזור — **פאנל שנעלם בשקט נראה שבור**.

---

## מסך ה-Study — מבנה

```
מדדים (4 כרטיסים, עוקבים אחרי הנושא הנבחר)
Studying  [All subjects] [נושא א 9] [נושא ב 5]     ← subject-picker
[Continue — 3 of 20] [Start fresh] [Create questions] [Review answered] [Manage]
┌── How well you judge yourself ──┬── You think you know these ──┐
│ 3 שורות דיוק לפי ביטחון          │ עד 5 פריטים + "and N more"   │
│ How often you say each (פס)      │                              │
│ Is the gap closing?              │                              │
│ Does rushing cost you?           │                              │
│ [השוואת נושאים — רק ב-All]        │                              │
└─────────────────────────────────┴──────────────────────────────┘
```

**כל הפאנלים עוקבים אחרי הנושא הנבחר.** הכל מרוכז ב-`applyStudyScope()` שקורא מ-`lastStudyStats` — **בלי קריאת שרת**. מעבר בין נושאים היה קורא ל-`loadStudyHome()` שטוען את כל הדק; זה היה העיכוב של 1–2 שניות.

תג הסימון בתפריט הצד נשאר על **כל** הדק בכוונה: משהו דחוף בקורס אחר הוא עדיין משהו דחוף.

---

## סשן שמור — לכל נושא בנפרד

`localStorage['mindsync.activeSessions']` הוא **מפה לפי קטגוריה**:

```js
{ "מבני נתונים": { ids, index, session, category, savedAt }, "תכנות מתקדם": {...} }
```

- `ALL_SUBJECTS = ''` הוא המפתח לסשן חוצה-נושאים
- יש המרה חד-פעמית מהפורמט הישן (`mindsync.activeSession`) — **אל תמחקו אותה בלי לוודא שעברה שנה**
- `studyState.category` = הנושא שאיתו הסשן **התחיל**, לא הנבחר כרגע. השניים נפרדים ברגע שמחליפים נושא בזמן שסשן מושהה
- סשן בן יותר מ-24 שעות נמחק (הלו"ז זז)

**אין באנר.** הסשן המושהה חי בתוך כפתור ההתחלה: `Start session` ↔ `Continue — 3 of 20`, ולידו `Start fresh` (`btn-ghost-danger`, אדום מלא). באנר נפרד גרם לדף לקפוץ בכל מעבר נושא.

`renderResumeBanner()` נקראת מתוך `saveSessionProgress` ו-`clearSessionProgress` עצמן — **הכפתור הוא תצוגה של האחסון**. הגרסה הקודמת ניסתה לסנכרן ידנית ושכחה שלושה מסלולים.

---

## רינדור טקסט ששייך למסמך

`renderRichText(el, text)` ב-`renderer.js` — **כל** טקסט שמקורו במסמך עובר דרכה (שאלה, תשובה, מסך סקירה, רשימת הטעויות).

- גדרות ` ``` ` מנצחות; בלעדיהן יש היוריסטיקה שמרנית (`CODE_LINE_RE`) שמקבצת שורות רצופות שנראות כמו קוד. שורה בודדת לעולם לא הופכת לבלוק — פסקה שמזוהה בטעות כקוד נראית שבורה.
- `reindentCode()` בונה מחדש פריסה מהתחביר כשיש שורה >90 תווים. עוקב אחרי מחרוזות (`"iOS"`) וסוגריים עגולים (`for (i=0; i<n; i++)`). Python ופסאודו-קוד עוברים ללא נגיעה.
- `<pre class="code-block" dir="ltr">` עם `unicode-bidi: isolate` ו-`white-space: pre-wrap`.

**למה זה היה שבור:** ירידות השורה תמיד היו במסד הנתונים. `textContent` לתוך `div` מקפל אותן לרווחים, ו-RTL סידר מחדש את הסימנים.

### בידוד דו-כיווני

`bidiName(text)` עוטף כל **שם נושא** שנכנס לטקסט אנגלי ב-`<span class="bidi-name" dir="auto">`. בלי זה הכיוון של השם דולף החוצה וגורר איתו סוגריים ואחוזים. משפט ה-verdict פורק לשורות כי שני שמות באורך 80 תווים נשברים באמצע גם עם בידוד.

---

## Gemini — יציבות

### שרשרת מודלים

```js
const GEMINI_FALLBACK_CHAIN = [
  'gemini-3.8-flash', 'gemini-3.7-flash', 'gemini-3.6-flash',
  'gemini-3.5-flash', 'gemini-3.5-flash-lite', 'gemini-3.1-flash-lite'
];
const DEFAULT_GEMINI_MODEL = GEMINI_FALLBACK_CHAIN[0];
```

- **ראש הרשימה הוא גם ברירת המחדל.** להוסיף מודל חדש בראש = השינוי היחיד שצריך.
- **מודלי 2.x לא מוגשים יותר.** נוסו ונכשלו. אל תחזירו אותם.
- Lite בזנב **בכוונה**: מאגר רווי במודל כבד מתאושש תוך 30–120 דקות, Lite תוך 5–15. שאלות חלשות שעוברות במסך האישור עדיפות על אפס שאלות.
- `resolveGeminiModel(cfg)` מתעלם משם מודל ששמור ב-`ai-config.json` אם הוא אחד מהדיפולטים שלנו — **אין בורר מודל בממשק**, אז ערך שמור הוא שאריות ולא בחירה. `"modelPinned": true` הוא דלת המילוט.

### Retry

4 נסיונות למודל, 2→4→8 שניות (~15ש'), ואז המודל הבא. **קצר בכוונה** — הרווח הוא להגיע למאגר אחר, לא להמתין למאגר מלא. מכבד `Retry-After`. 400/403 נכשלים מיד.

`503` הוא **קיבולת משותפת**, לא מכסה — תשלום לא עוזר. `429` כן קשור למכסה.

### דיווח התקדמות

`onProgress` → `main.js` → `event.sender.send('ai-progress')` → `aiProgress` overlay ב-`renderer.js`, עם ספינר, הודעה חיה ושעון. אחרי 75 שניות ההודעה מתחלפת. ההסתרה ב-`finally`.

### תיקון JSON

`escapeControlCharsInJsonStrings()` ב-`main.js` — מודלים שמים ירידות שורה אמיתיות בתוך מחרוזות כשהתוכן הוא קוד, ו-`JSON.parse` נופל. הפונקציה עוקבת אחרי האם היא בתוך literal ומבריחה תווי בקרה שם בלבד. JSON תקין עובר ללא שינוי.

`parseModelJson()` מנסה → מתקן → ואם נכשל מדפיס את **המיקום המדויק** עם הקשר ושומר את התשובה המלאה ל-`app.getPath('temp')`. קריאה של 50 שניות לא ניתנת לשחזור.

---

## פילטרים ב-`main.js`

| פונקציה | מה נחסם |
|---|---|
| `isSelfContained()` | "הפונקציה שתוכתב", "לפי הטקסט" |
| `isHistoricalTrivia()` | "באיזו עיר", "מי הוכיח" |
| `needsReasoningNotQuote()` | "הסבר את תשובתך" |
| `isExerciseTrivia()` | "מהו סוג הנתונים" |
| `looksLikeOutline()` / `looksLikeHeading()` | שקופיות אג'נדה, כותרות |
| `analyseAnswerText()` | נזק גופן, נוסחה בלבד |
| `quoteAnswersQuestion()` | סמל בשאלה חסר בציטוט |
| `cleanMathNotation()` | LaTeX → סמלים אמיתיים |
| `stripMultipleChoice()` | רשימות תשובות |

**`cleanMathNotation`:** המפה **לא** מכילה בקסלאשים — הם מתווספים בבניית ה-regex במקום אחד. `'\\in'` הופך ל-`\i` = `i`, וזה החליף `Main` ב-`Ma∈`.

**`stripMultipleChoice` נכתב מחדש:** מוצא כל סמן (`א.` / `א)` / `(a)`) וגוזר רק כשיש **שלושה לפי הסדר**. הגרסה הישנה תפסה רק `"א) ..."` בתחילת שורה ופספסה רשימות inline. בלי `\b` בשום מקום.

הפרומפט אוסר במפורש על אפשרויות תשובה ומבקש גדרות קוד — חסימה בשני מקומות.

---

## ביצועים — פרויקציות קלות

| מסלול | מה נחסך |
|---|---|
| `GET /api/study?light=1` | `select('-reviews')` — מסך הניהול לא קורא היסטוריה |
| `GET /api/files?light=1` | `select('-content')` — רשימת שמות שלפה את כל הטקסט של כל PDF |
| `get-file` (IPC חדש) | תוכן של קובץ אחד, רק במסלול שצריך אותו |

בנוסף: `loadManageList` ו-`openBrowseScreen` מציירים `ms-skeleton` **לפני** ה-`await`. הסדר ההפוך השאיר מסך קפוא שנראה תקוע.

---

## מסך הסקירה (`study-browse`)

שלוש לשוניות לפי **התוצאה האחרונה**: `missed`/`wrong` → Got it wrong · `partial`/`stuck` → Partly · `got_it`/`solved` → Got it right.

**קריאה בלבד.** פתיחת שאלה לא נרשמת ולא מזיזה תזמון — אחרת דפדוף היה מזהם את ההיסטוריה שכל מספרי הכיול בנויים עליה. אם הלשונית הפעילה ריקה, המסך קופץ לראשונה שיש בה תוכן.

---

## מצב איכות לפי סוג חומר

| חומר | תוצאה |
|---|---|
| סטטיסטיקה, ארכיטקטורות, תורת גרפים | ✅ מצוין |
| אלגברה לינארית | ✅ עובד |
| תכנות מונחה עצמים | ✅ עובד (אחרי תיקון ה-JSON והמעצב) |
| לוגיקה עתירת נוסחאות | ⚠️ חלקי |

---

## מה נשאר

### הדבר הבא שדובר עליו
1. **`thinkingSeconds`** — `secondsSpent` מודד את **כל** המחזור כולל קריאת התשובה, אז `pacePattern` מודד בעיקר את אורך התשובה. הזמן שמעניין הוא מהצגת השאלה ועד לחיצה על כפתור הביטחון. דורש: שדה ב-`reviewSchema`, חותמת זמן ב-`revealAnswer()`, העברה ב-IPC. נתונים ישנים לא יכללו אותו.

### חוסמי הפצה
2. **`nodeIntegration: true` + `contextIsolation: false`** ב-`main.js` — תוכן PDF חיצוני נכנס ל-`innerHTML` ברנדרר עם גישה מלאה ל-Node. נדרש `preload.js` + `contextBridge` ומעבר על כל קריאת `ipcRenderer`. **החוסם האמיתי היחיד להפצה.**
3. **אין הפרדת משתמשים בשרת** — `Task.find()` מחזיר הכל. נדרש `User`, JWT, `userId`.

### פיצ'רים
4. **תור עבודות מתמיד ליצירה** — כרגע כישלון מוחק תהליך של 50 שניות. שמירת עבודה ממתינה + ניסיון חוזר ברקע כל 10 דקות + התראה. מתאים בדיוק לחלון ההתאוששות של 30–120 דקות. **ההמלצה החזקה ביותר שלי.**
5. **פיצול מסמך לחלקים** — בקשה אחת על 30 עמודים היא נקודת כשל אחת; חמש בקשות משאירות תוצאה חלקית.
6. **חיבור הדק לתאריך המבחן** — יש `Event` עם סיווג `exam` ויש `category` על כל שאלה; חסר החיבור. "סטטיסטיקה — בעוד 9 ימים. בקצב הנוכחי תגיע ל-21 מתוך 34." שאלה פתוחה: התאמה לפי שם או בחירה ידנית.
7. **קיצור זמן לשאלה הראשונה** — דק לדוגמה מוכן מראש. הערך מצטבר, אבל רק אם המשתמש שורד את היום הראשון.

### שיפורים
8. RTL בממשק · 9. נגישות (אפס `aria-label`) · 10. פיצול `renderer.js` · 11. ייצוא/גיבוי · 12. מיון משימות · 13. תיעוד במוצר: עובד הכי טוב עם סיכומי הרצאות

### אנטי־פיצ'ר
**אל תוסיפו צ'אט על שאלה.** זו בדיוק אשליית השטף שהתזה נלחמת בה. אם בכל זאת — רק **אחרי** שהדירוג נשמר.

---

## הפעלה

```bash
cd server && npm install && npm run dev   # חייב לרוץ ראשון
cd client && npm install && npm start
```

**דרוש (לא בגיט):** `server/.env` עם `MONGO_URI`, `client/credentials.json`, `client/token.json`

**Gemini:** מפתח מ-aistudio.google.com/apikey → Settings → AI engine → Test → Save.
פורמט `AQ.` הוא הנוכחי; `AIza` נדחה מספטמבר 2026. **אין לאמת לפי קידומת.**

**Gemini 3.x:** `temperature`/`top_p`/`top_k` deprecated רשמית — אל תשלחו. זורק שגיאה על penalty params. החשיבה נגבית מתקציב הפלט (`maxOutputTokens` נדיב + `thinkingLevel`). `mediaResolution: HIGH` חיוני לקריאת אינדקסים.

---

## איך לעבוד

באגים נתפסו **רק** כשהמשתמש הריץ בפועל ודיווח מדויק. לבנות פיצ'ר אחד, לבדוק, ואז להמשיך.

**בשאלות עיצוב ומוצר — לשאול לפני שכותבים קוד.** כתיבת קוד לפני החלטה מבזבזת טוקנים ולפעמים מובילה לכיוון הלא נכון.

המשתמש סטודנט למערכות מידע, מתכנת מתחיל. לענות בעברית, קוד באנגלית. הסברים ספציפיים, בלי להניח ידע מובן מאליו. לסדר טקסט כך שעברית ואנגלית יהיו נוחים לקריאה.